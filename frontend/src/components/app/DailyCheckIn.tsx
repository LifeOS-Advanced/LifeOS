/**
 * Legacy quick check-in — no longer auto-opens.
 * Morning entry is unified through Daily Start (/app/daily-start).
 * Evening check-in data is written when Evening Shutdown saves (API users).
 */
export function DailyCheckInModal() {
  return null;
}
