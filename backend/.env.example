MONGODB_URI=mongodb://localhost:27017/lifeos
PORT=5000
NODE_ENV=development
JWT_SECRET=change_me_access_secret
JWT_REFRESH_SECRET=change_me_refresh_secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d
CLIENT_URL=http://localhost:8080,http://localhost:5173,http://127.0.0.1:8080

# GitHub OAuth (required for GitHub sign-in — same app as frontend VITE_GITHUB_CLIENT_ID)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Optional override (must match GitHub app callback URL exactly):
# GITHUB_REDIRECT_URI=http://localhost:8080/auth/github/callback
